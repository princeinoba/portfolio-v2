{
  "name": "prince-inoba-portfolio",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A dependency-light, statically generated portfolio optimized for Vercel.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "verify": "npm run test && npm run build && node scripts/verify-build.mjs",
    "check": "npm run verify"
  }
}
